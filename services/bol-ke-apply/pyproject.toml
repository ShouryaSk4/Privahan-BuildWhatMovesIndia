[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bol-ke-apply"
version = "0.1.0"
description = "Module 6: Bol Ke Apply - MCP server voice/conversational front door"
requires-python = ">=3.11"
dependencies = [
    "parivahan-contracts",
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.7.0",
]

[tool.uv.sources]
parivahan-contracts = { workspace = true }
