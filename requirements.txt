# Vercel serverless function dependencies (api/index.py).
# Local development uses uv (see README); this file exists because Vercel's
# Python runtime installs with pip. The local path entries provide the
# parivahan-* packages the service requirements reference.
fastapi>=0.115
httpx>=0.27
pydantic>=2.7
./packages/contracts
./services/journey
./services/gateway
./services/identity
./services/academy
./services/bol-ke-apply
